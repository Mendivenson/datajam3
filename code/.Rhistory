curve(models[[model]](x, rango = 5, sigma = 5, alpha = 1), 0, 70, ylab = "", xlab = "h",
col = "darkblue",add = F,
ylim = c(0,7), main = stringr::str_to_upper(model), lwd = 2, xaxt = 'n', yaxt = 'n')
}))
models <- list(
# ---> Efecto pepita
nugget       = function(h, rango = 1, sigma = 2, ...){
rep(sigma, length(h))
},
# ---> Modelo triangular
triangular   = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1     <- vg < rango
c2     <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma * h[c1] / rango
vg[c2] <- sigma
vg
},
# ---> Modelo circular
circular    = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1    <- vg < rango
c2    <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma * (1.5*(h[c1]/rango) - 0.5*((h[c1]**3)/(rango**3)))
vg[c2] <- sigma
vg
},
# ---> Modelo esférico
spherical   = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1    <- vg < rango
c2    <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma - sigma * (1 - 1.5 * h[c1]/rango + 0.5 * (h[c1]/rango)^3)
vg[c2] <- sigma
vg
},
# ---> Modelo cúbico
cubical     = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1    <- vg < rango
c2    <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma*(7 * (h[c1]/rango)**2 - 8.75 * (h[c1]/rango)**3 + 3.5 * (h[c1]/rango)**5 - 0.75 * (h[c1]/rango) ** 7)
vg[c2] <- sigma
vg
},
# ---> Modelo exponencial
exponential = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma - sigma * exp(- h/rango)
vg
},
wave        = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma-sigma*(rango/h)*sin(h/rango)
vg
},
# ---> Modelo pentamodel
pentamodel  = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1     <- vg < rango
c2     <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma * ( (22/3) * (h[c1]/rango)^2 - 33 * (h[c1]/rango)^4 + 38.5 * (h[c1]/rango)^5 -
16.5 * (h[c1]/rango)^7 + 5.5 * (h[c1]/rango)^9 - (5/6) * (h[c1]/rango)^11)
vg[c2] <- sigma
vg
},
# ---> Modelo radon 2
`radon ord 2`   = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma-sigma*(1 + h/rango)*exp(-h/rango)
vg
},
# ---> Modelo radon 4
`radon ord 4`   = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma-sigma*(1 + h/rango + (1/3) * (h/rango)^2) * exp(-h/rango)
vg
},
# ---> Modelo gaussiano
gaussian   = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma - sigma * exp(- (h/rango)^2)
vg
},
# ---> Modelo Lanteujoul
lanteujoul   = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma - sigma * sin(pi/2 * exp(- abs(h)/rango))
vg
}
)
par(mfrow = c(3,4), mar = c(0.5, 0.5, 1.4, 0.5))
invisible(
lapply(names(models), \(model) {
curve(models[[model]](x, rango = 5, sigma = 5, alpha = 1), 0, 70, ylab = "", xlab = "h",
col = "darkblue",add = F,
ylim = c(0,7), main = stringr::str_to_upper(model), lwd = 2, xaxt = 'n', yaxt = 'n')
}))
models <- list(
# ---> Efecto pepita
nugget       = function(h, rango = 1, sigma = 2, ...){
rep(sigma, length(h))
},
# ---> Modelo triangular
triangular   = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1     <- vg < rango
c2     <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma * h[c1] / rango
vg[c2] <- sigma
vg
},
# ---> Modelo circular
circular    = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1    <- vg < rango
c2    <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma * (1.5*(h[c1]/rango) - 0.5*((h[c1]**3)/(rango**3)))
vg[c2] <- sigma
vg
},
# ---> Modelo esférico
spherical   = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1    <- vg < rango
c2    <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma - sigma * (1 - 1.5 * h[c1]/rango + 0.5 * (h[c1]/rango)^3)
vg[c2] <- sigma
vg
},
# ---> Modelo cúbico
cubical     = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1    <- vg < rango
c2    <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma*(7 * (h[c1]/rango)**2 - 8.75 * (h[c1]/rango)**3 + 3.5 * (h[c1]/rango)**5 - 0.75 * (h[c1]/rango) ** 7)
vg[c2] <- sigma
vg
},
# ---> Modelo exponencial
exponential = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma - sigma * exp(- h/rango)
vg
},
wave        = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma-sigma*(rango/h)*sin(h/rango)
vg
},
# ---> Modelo pentamodel
pentamodel  = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1     <- vg < rango
c2     <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma * ( (22/3) * (h[c1]/rango)^2 - 33 * (h[c1]/rango)^4 + 38.5 * (h[c1]/rango)^5 -
16.5 * (h[c1]/rango)^7 + 5.5 * (h[c1]/rango)^9 - (5/6) * (h[c1]/rango)^11)
vg[c2] <- sigma
vg
},
# ---> Modelo radon 2
`radon ord 2`   = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma-sigma*(1 + h/rango)*exp(-h/rango)
vg
},
# ---> Modelo radon 4
`radon ord 4`   = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma-sigma*(1 + h/rango + (1/3) * (h/rango)^2) * exp(-h/rango)
vg
},
# ---> Modelo gaussiano
gaussian   = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma - sigma * exp(- (h/rango)^2)
vg
},
# ---> Modelo Lanteujoul
lanteujoul   = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma - sigma * sin(pi/2 * exp(- abs(h)/rango))
vg
}
)
par(mfrow = c(3,4), mar = c(0.2, 0.2, 1.4, 0.2))
invisible(
lapply(names(models), \(model) {
curve(models[[model]](x, rango = 5, sigma = 5, alpha = 1), 0, 70, ylab = "", xlab = "h",
col = "darkblue",add = F,
ylim = c(0,7), main = stringr::str_to_upper(model), lwd = 2, xaxt = 'n', yaxt = 'n')
}))
models <- list(
# ---> Efecto pepita
nugget       = function(h, rango = 1, sigma = 2, ...){
rep(sigma, length(h))
},
# ---> Modelo triangular
triangular   = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1     <- vg < rango
c2     <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma * h[c1] / rango
vg[c2] <- sigma
vg
},
# ---> Modelo circular
circular    = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1    <- vg < rango
c2    <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma * (1.5*(h[c1]/rango) - 0.5*((h[c1]**3)/(rango**3)))
vg[c2] <- sigma
vg
},
# ---> Modelo esférico
spherical   = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1    <- vg < rango
c2    <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma - sigma * (1 - 1.5 * h[c1]/rango + 0.5 * (h[c1]/rango)^3)
vg[c2] <- sigma
vg
},
# ---> Modelo cúbico
cubical     = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1    <- vg < rango
c2    <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma*(7 * (h[c1]/rango)**2 - 8.75 * (h[c1]/rango)**3 + 3.5 * (h[c1]/rango)**5 - 0.75 * (h[c1]/rango) ** 7)
vg[c2] <- sigma
vg
},
# ---> Modelo exponencial
exponential = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma - sigma * exp(- h/rango)
vg
},
wave        = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma-sigma*(rango/h)*sin(h/rango)
vg
},
# ---> Modelo pentamodel
pentamodel  = function(h, rango = 1, sigma = 2, ...){
vg     <- h
# Caso 1 (c1): Antes de alcanzar el rango, Caso 2 (c2): Después del rango
c1     <- vg < rango
c2     <- !c1
# Cálculo de la covarianza
vg[c1] <- sigma * ( (22/3) * (h[c1]/rango)^2 - 33 * (h[c1]/rango)^4 + 38.5 * (h[c1]/rango)^5 -
16.5 * (h[c1]/rango)^7 + 5.5 * (h[c1]/rango)^9 - (5/6) * (h[c1]/rango)^11)
vg[c2] <- sigma
vg
},
# ---> Modelo radon 2
`radon ord 2`   = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma-sigma*(1 + h/rango)*exp(-h/rango)
vg
},
# ---> Modelo radon 4
`radon ord 4`   = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma-sigma*(1 + h/rango + (1/3) * (h/rango)^2) * exp(-h/rango)
vg
},
# ---> Modelo gaussiano
gaussian   = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma - sigma * exp(- (h/rango)^2)
vg
},
# ---> Modelo Lanteujoul
lanteujoul   = function(h, rango = 1, sigma = 2, ...){
vg  <- h
# Cálculo de la covarianza
vg  <- sigma - sigma * sin(pi/2 * exp(- abs(h)/rango))
vg
}
)
class(models) <- 'models.SpatFD'
par(mfrow = c(3,4), mar = c(0.2, 0.2, 1.4, 0.2))
invisible(
lapply(names(models), \(model) {
curve(models[[model]](x, rango = 5, sigma = 5, alpha = 1), 0, 70, ylab = "", xlab = "h",
col = "darkblue",add = F,
ylim = c(0,7), main = stringr::str_to_upper(model), lwd = 2, xaxt = 'n', yaxt = 'n')
}))
models |> object.size() |>  format('MB')
# url <-
library(archive)
library(data.table)
# archive("https://storage.googleapis.com/gtfs-estaticos/GTFS_20260727.zip")
# Abrir una conexión de streaming a un archivo específico dentro del zip
con <- archive_read("https://storage.googleapis.com/gtfs-estaticos/GTFS_20260727.zip",
file = 'stop_times.txt',
format = 'zip')
stop.times <- fread(con)
# url <-
library(archive)
library(data.table)
# archive("https://storage.googleapis.com/gtfs-estaticos/GTFS_20260727.zip")
# Abrir una conexión de streaming a un archivo específico dentro del zip
con <- archive_read("https://storage.googleapis.com/gtfs-estaticos/GTFS_20260727.zip",
file = 'stop_times.txt',
format = 'zip')
stop.times <- read.csv(con)
con <- archive_read("https://storage.googleapis.com/gtfs-estaticos/GTFS_20260727.zip",
file = 'routes.txt',
format = 'zip')
routes <- read.csv(con)
close(con)
routes |> View()
stop.time |> head()
stop.times |> head()
con <- archive_read("https://storage.googleapis.com/gtfs-estaticos/GTFS_20260727.zip",
file = 'trips.txt',
format = 'zip')
trips <- read.csv(con)
trips |> View()
ft.stop.times <- as.data.table(stop.times)
ft.stop.times |> head()
rm(ft.stop.times)
library(dplyr)
stop.times |> filter(trip_id == 'TD1920147.1.10036')
stop.times |> filter(stop_headsign == 'G45')
stop.times |> filter(stop_headsign == 'G45') |> order_by(stop_id)
stop.times |> filter(stop_headsign == 'G45') |> arrange(stop_id)
stop.times |> filter(stop_headsign == 'G45') |> arrange(stop_id) |> View()
stop.times |> select(-arrival_time,-departure_time)
stop.times |> select(-arrival_time,-departure_time) |> distinct()
stop.times |> select(-arrival_time,-departure_time) |> distinct() |> View()
stop.times |> select(-arrival_time,-departure_time,-trip_id) |> distinct() |> View()
stop.times |> select(-arrival_time,-departure_time,-trip_id,-timepoint) |> distinct() |> View()
trips |> View()
routes |> View()
trips |> View()
stop.times |> colnames()
stop.times |> filter(stop_headsign %in% routes[agency_id == 1,]$route_short_name)
stop.times |> filter(stop_headsign %in% routes[routes$agency_id == 1,]$route_short_name)
stop.times |> filter(stop_headsign %in% routes[routes$agency_id == 1,]$route_short_name) |> select(stop_headsign)
stop.times |> filter(stop_headsign %in% routes[routes$agency_id == 1,]$route_short_name) |> select(stop_headsign)  |> unique()
stop.times |> filter(stop_headsign %in% routes[routes$agency_id == 1,]$route_short_name) |> select(stop_headsign)  |> unique() |> length()
stop.times |> filter(stop_headsign %in% routes[routes$agency_id == 1,]$route_short_name) |> select(stop_headsign)  |> c() |> unique() |> length()
stop.times |> filter(stop_headsign %in% routes[routes$agency_id == 1,]$route_short_name) |> select(stop_headsign) |> unique() |> length()
stop.times |> filter(stop_headsign %in% routes[routes$agency_id == 1,]$route_short_name) |> select(stop_headsign) |> distinct() |> nrow()
routes$route_short_name |> unique() |> length()
routes[routes$agency_id == 1,]$route_short_name |> unique() |> length()
routes[routes$agency_id == 1,]$route_id |> unique() |> length()
routes |> colnames()
setwd(dir = 'datajam/repositorio/')
setwd(dir = 'datajam/repositorio/code')
setwd(dir = 'code')
GTFS <- readLines('../data/GTFS.txt')
con <- archive_read("https://storage.googleapis.com/gtfs-estaticos/GTFS_20260727.zip",
file = 'routes.txt',
format = 'zip')
routes <- read.csv(con)
routes |> head()
# Abrir una conexión de streaming a un archivo específico dentro del zip
con <- archive_read("https://storage.googleapis.com/gtfs-estaticos/GTFS_20260727.zip",
file = 'stop_times.txt',
format = 'zip')
stop.times <- read.csv(con)
stop.times |> head()
rm(list = ls())
GTFS
GTFS <- readLines('../data/GTFS.txt')
GTFS
# ------------------- CREACIÓN DEL GRAFO REPRESENTACIÓN DEL SISTEMA TRONCAL DE TM ------------------
# Dentro de los datos disponibilizados en el portal de datos abiertos de Bogotá no existe información
# que permita reconstruir un grafo del sistema, pero dentro de la página de datos de TM se
# encuentra la información relacionada con el General Transit Feed Specification (GTFS) que entre otras
# cosas contiene un listado de las rutas y sus paradas que será lo que se use para construir el grafo.
#
# Además de dicha información se asignará a cada nodo del grado las características de entradas y
# salidas (Estos datos sí son del portal de datos abiertos) para generar un proxy de los puntos de
# partida y salida de los usuarios del sistema.
#
# - GTFS TM:
#   https://datosabiertos-transmilenio.hub.arcgis.com/search?q=GTFS&sort=Date%20Updated%7Cmodified%7Cdesc
#
# - Validaciones hechas en el sistema:
#   https://datosabiertos.bogota.gov.co/dataset/validaciones-diarias-sitp
#
# - Salidas registradas del sistema:
#   https://datosabiertos.bogota.gov.co/dataset/consolidado-de-salidas-sistema-troncal-por-franja-horaria
#
# SALIDA:
# Un archivo representando el grafo de TM donde:
#
# - NODOS: Cada nodo representa una estación del sistema troncal acompañado de longitud, latitud, ID
#          de la estación, demanda y salidas de la estación respectivamente.
# - ARISTAS: Cada arista tendrá como peso la cantidad de conexiones directas entre dos estaciones.
#
# Además de esto, se debe tener un listado de rutas + estaciones para poder restar a las aristas
# correspondientes el peso generado por el servicio sin necesidad de generar el grafo desde cero
# sobre todo pensando en la implementación de herramientas de visualización más adelante.
library(archive)
library(dplyr)
# library(data.table)
# archive("https://storage.googleapis.com/gtfs-estaticos/GTFS_20260727.zip")
GTFS <- readLines('../data/GTFS.txt')
# Abrir una conexión de streaming a un archivo específico dentro del zip
con <- archive_read(GTFS,
file = 'stop_times.txt',
format = 'zip')
stop.times <- read.csv(con)
con <- archive_read(GTFS,
file = 'routes.txt',
format = 'zip')
routes <- read.csv(con)
con <- archive_read(GTFS,
file = 'trips.txt',
format = 'zip')
trips <- read.csv(con)
# ------------------- CREACIÓN DEL GRAFO REPRESENTACIÓN DEL SISTEMA TRONCAL DE TM ------------------
# Dentro de los datos disponibilizados en el portal de datos abiertos de Bogotá no existe información
# que permita reconstruir un grafo del sistema, pero dentro de la página de datos de TM se
# encuentra la información relacionada con el General Transit Feed Specification (GTFS) que entre otras
# cosas contiene un listado de las rutas y sus paradas que será lo que se use para construir el grafo.
#
# Además de dicha información se asignará a cada nodo del grado las características de entradas y
# salidas (Estos datos sí son del portal de datos abiertos) para generar un proxy de los puntos de
# partida y salida de los usuarios del sistema.
#
# - GTFS TM:
#   https://datosabiertos-transmilenio.hub.arcgis.com/search?q=GTFS&sort=Date%20Updated%7Cmodified%7Cdesc
#
# - Validaciones hechas en el sistema:
#   https://datosabiertos.bogota.gov.co/dataset/validaciones-diarias-sitp
#
# - Salidas registradas del sistema:
#   https://datosabiertos.bogota.gov.co/dataset/consolidado-de-salidas-sistema-troncal-por-franja-horaria
#
# SALIDA:
# Un archivo representando el grafo de TM donde:
#
# - NODOS: Cada nodo representa una estación del sistema troncal acompañado de longitud, latitud, ID
#          de la estación, demanda y salidas de la estación respectivamente.
# - ARISTAS: Cada arista tendrá como peso la cantidad de conexiones directas entre dos estaciones.
#
# Además de esto, se debe tener un listado de rutas + estaciones para poder restar a las aristas
# correspondientes el peso generado por el servicio sin necesidad de generar el grafo desde cero
# sobre todo pensando en la implementación de herramientas de visualización más adelante.
library(archive)
library(dplyr)
# library(data.table)
# archive("https://storage.googleapis.com/gtfs-estaticos/GTFS_20260727.zip")
GTFS <- readLines('../data/GTFS.txt')
# Abrir una conexión de streaming a un archivo específico dentro del zip
con <- archive_read(GTFS,
file = 'routes.txt',
format = 'zip')
routes <- read.csv(con)
con <- archive_read(GTFS,
file = 'trips.txt',
format = 'zip')
trips <- read.csv(con)
options(timeout = 120)
con <- archive_read(GTFS,
file = 'stop_times.txt',
format = 'zip')
stop.times <- read.csv(con)
stop.times |> head()
